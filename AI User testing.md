# AI User Testing

> (구 「公共サイト監査サービス」 / 내부 코드네임: 区役所ダンジョン)
> 최종 갱신: 2026-08-08 (AI HACK 2026 Day 1)
> 상태: **포지셔닝 확정.** AI User Testing 제품 + 공공 벤치마크의 듀얼 구조.

---

## 0. TL;DR

**한 줄 정의**
> 제약이 강제된 AI 사용자 10명을 웹사이트에 투입해, 실제 고객이 어디서 이탈하는지 찾아낸다.

| | 기존 UX 테스트 | 이 제품 |
|---|---|---|
| 피험자 | 사람 10명 | **제약 스펙 하의 AI 10명** |
| 수집 | 인터뷰 + 관찰 | **행동 로그 (클릭 경로·이탈 지점)** |
| 산출 | 리서처 분석 리포트 | **점수 + 재현 가능한 실패 트레이스 + 개선안** |
| 비용/시간 | 수백만원 / 수 주 | 자동 / 수 시간 |
| 재현성 | 없음 (표본이 매번 다름) | **있음 (스펙 버전 고정)** |

| 항목 | 판정 |
|---|---|
| 컨셉 (제약 강제 에이전트 = AI 피험자) | **강함.** 코어 |
| 제약 레이어 | **유일한 moat.** 여기에 개발 시간을 몰빵 (§5) |
| 페르소나 md 30개 | 대부분 연출. 행동을 바꾸는 5개만 실질 (§5) |
| browser-use를 인프라로 사용 | **판단 옳음.** 단 관측 파이프라인 훅 가능성이 급소 (§6-R8) |
| 듀얼 타깃 (B2B 매출 + 공공 화제성) | **확정.** 심사위원 4인 전원 커버 (§7) |
| 검증 (실사용자 대조) | **미해결.** 소규모라도 반드시 (§5) |

**한 줄 총평:** 「사이트 監査」가 아니라 「AI User Testing」으로 정의한 순간 제품이 됐다. 남은 급소는 제약 레이어의 구현 가능성 하나.

---

## 1. 아이디어 개요

### 문제
일본 공공·법인 웹사이트는 "규격에는 합격하지만 실제로는 못 쓰는" 상태다.

- 公的機関은 JIS X 8341-3 (WCAG 기반) 시험 결과를 공개한다 → 대부분 **AA 준수 자기선언**
- 하지만 그 검사는 axe-core, Lighthouse, Nu HTML Checker 같은 **규칙 기반**이다
- 규칙 기반은 "alt 속성이 있는가"는 보지만 **"72세가 転入届을 끝낼 수 있는가"는 안 본다**
- 실제 UX 평가는 사람이 휴리스틱·인터뷰로 하고, 비싸고, 표본이 작고, 전국 비교가 불가능하다

**→ 「準拠している」와 「実際に使える」 사이에 아무도 측정하지 않는 간극이 있다.**

### 해결
제약 조건이 부여된 AI 에이전트가 페르소나로서 실제 사이트를 탐색하고, **어디서 어떻게 실패했는지**를 재현 가능한 형태로 기록한다.

### 파이프라인
```
페르소나 지식베이스 (md)
        ↓
제약 레이어 (어휘 마스킹 / 시야 / 조작 / 인내 예산)   ← 핵심
        ↓
브라우저 에이전트 (Playwright)  →  대상 사이트
        ↓
행동 트레이스 (클릭 경로, 막힌 지점, 포기 시점)
        ↓
지표 산출 + 개선안 생성 + 리플레이 영상
```

---

## 2. 미션 설계 (읽기 전용 원칙)

**절대 하지 않는 것:** 회원가입, 마이넘버 인증, API 신청, 폼 제출, 로그인.
→ ToS 위반·업무방해 리스크 전부 여기서 발생한다. 공개 www 안에서만 움직인다.

**하는 것:** 「この業務をやろうとして、どこまで行けるか」

| 미션 예시 | 측정되는 것 |
|---|---|
| 外国人が転入届に必要な書類を調べる | 탐색 도달, 다국어 패리티 |
| 児童手当の申請期限を確認する | 정보 발견 가능성 |
| 粗大ごみの出し方を調べる | 생활 밀착 태스크 |
| 引っ越し後の手続き一覧を把握する | 정보 구조화 수준 |

### 지표 — 에이전트가 아니면 못 하는 것 / 기존 툴로 되는 것을 구분할 것

**에이전트 전용 (여기가 상품)**
- **태스크 성공률** — 자력으로 답에 도달했는가
- **도달 클릭 수 / 행き止まり 횟수 / 왕복 횟수**
- **포기 지점** — 인내 예산 소진 시점과 그때의 화면
- **언어 패리티** — *같은 미션*을 영어/중국어로 완수 가능한가 (영어 페이지 존재 여부 ❌ / 같은 답에 도달 ⭕)
- **PDF 의존율** — 답이 PDF 안에만 있는 비율 ← 아무도 발표한 적 없는 지표
- **에이전트 친화성 (Agent Readiness)** — 구조화 마크업, 시맨틱, 조작 가능성

**기존 툴로 커버 (리포트 하단에 한 줄, 헤드라인 금지)**
- Lighthouse: 속도, LCP
- axe-core: WCAG 규칙 위반
- ※ 이걸 앞에 내세우면 "그거 Lighthouse 아닌가요?"로 프로젝트 전체가 래퍼로 격하된다

---

## 3. 특장점 (진짜 차별점)

1. **準拠 vs 実使用 갭을 숫자로 만든다**
   자치체는 이미 「AA準拠」를 공개 선언하고 있다. 그 선언과 에이전트 실측을 **자동 대조**할 수 있다.
   → 「AA準拠と自己宣言しているが、外国人ミッションは踏破できなかった」
   이 대조는 일본 공공 특유의 제도적 맥락이 있어야만 성립한다. 해외 툴이 흉내낼 수 없다.

2. **실패 로그가 산출물이다 (점수가 아니라)**
   Lighthouse: 「LCP 4.2秒」
   이것: 「転入届を探して くらしの情報 → 各種手続き → 住民異動 と3階層降りたが、答えはトップの「よくある質問」にあった。7クリック、行き止まり2回、自力到達失敗」
   → 課長이 읽고 바로 움직일 수 있는 문장. 이걸 만드는 건 현재 아무도 없다.

3. **제약 레이어** (§5) — 페르소나를 프롬프트가 아니라 도구 레벨에서 구현
4. **전국 동일 기준 비교** — 사람 평가로는 물리적으로 불가능한 스케일

---

## 4. 시장 조사 결과 (본인 조사 + 보강)

**확인된 것**
- 公的機関의 JIS X 8341-3 시험 결과 공개는 「みんなの公共サイト運用ガイドライン」 하에서 실질 관행 → **규칙 기반 검사**
- 일본 UI/UX·행정DX 컨설팅은 다수 존재하나 **사람 휴리스틱 + 인터뷰** 중심
- 에이전트가 자동으로 탐색→업무수행→점수산정까지 하는 서비스는 국내에서 거의 안 보임

**보강 — 신규성 주장은 좁혀야 한다 (要検証 포함)**
- 해외에는 **synthetic user research** 카테고리가 이미 존재한다 (AI 페르소나로 UX 테스트). 신규가 아니다.
- 그리고 그 카테고리는 UX 리서치 커뮤니티에서 **신뢰성 비판을 받고 있다.** 「AI 페르소나로 UX 평가」라고 말하는 순간 아는 사람은 경계한다.
- 에이전틱 브라우저 자동화 (browser-use, Skyvern 등)도 기성품이 있다.
*browser-use: AI 에이전트가 웹 브라우저를 사람처럼 제어하여 웹사이트를 탐색하고 클릭, 폼 입력, 데이터 추출 등의 작업을 자율적으로 수행하도록 돕는 오픈소스 프레임워크 https://www.itworld.co.kr/article/3813900/ai-%EC%97%90%EC%9D%B4%EC%A0%84%ED%8A%B8%EB%A5%BC-%EC%9C%84%ED%95%9C-%EC%9B%B9-%EB%B8%8C%EB%9D%BC%EC%9A%B0%EC%A0%80-%EC%9E%90%EB%8F%99%ED%99%94-%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8-%EB%B8%8C.html
- 접근성 검증 전문 벤더도 일본에 존재한다 (要検証: ミツエーリンクス, インフォアクシア 등)

**→ 주장해야 할 신규성은 "AI 페르소나 UX 테스트"가 아니라:**
> **일본 공공 특유의 제약(카타카나 외래어, PDF 의존, 準拠 자기선언 제도)에 맞춰 설계된 제약 레이어 + 준거-실사용 갭 대조**

넓게 주장하면 팩트체크 당한다. 좁게 주장하면 방어된다.

---

## 5. 페르소나 레이어 — 핵심이자 가장 약한 고리

### 옳은 부분
- 얇은 프롬프트 페르소나(「72세, 스마트폰 서툶」)가 일반론만 뱉는다는 관찰 → **맞다**
- 페르소나 지식과 평가 결과를 **분리 저장**해서 교차 분석 → **아키텍처적으로 옳다.** 유지할 것

### 치명적 문제: 지식베이스는 능력을 *추가*하지, *제거*하지 못한다

「영어를 못 읽는다」「카타카나 영어를 모른다」는 **결핍**이다.
그런데 기반 LLM은 영어를 유창하게 읽고 「マイナポータル」가 뭔지 안다.
md 파일 30개로 "나는 영어를 모른다"고 아무리 써줘도 → **모르는 척 연기**할 뿐이고, 연기는 새어나온다.
"이게 무슨 뜻인지 모르겠다"고 말한 직후에 정확한 페이지로 이동해버린다.

**초능력자에게 무능을 연기시키는 문제다. 컨텍스트를 더 넣을수록 오히려 악화된다. 필요한 건 덧셈이 아니라 뺄셈이다.**

### 해법: 제약 레이어 (도구 계층에서 정보를 실제로 훼손)

| 제약 | 구현 | 효과 |
|---|---|---|
| **어휘 마스킹** | 페이지 텍스트를 에이전트에 넘기기 전, 페르소나 어휘목록(JLPT 등급표 / 카타카나 외래어 리스트) 밖 단어를 마스킹. `オンライン申請` → `オンライン◯◯` | 진짜로 모르는 상태가 됨 |
| **시야 제약** | 브라우저 줌 200%, 뷰포트 축소, **DOM 텍스트 차단하고 스크린샷만 제공** | 고령자의 정보 획득량 재현 |
| **조작 제약** | Ctrl+F 금지, 사이트 내 검색창 금지, 뒤로가기 N회 제한, 페이지당 체류시간 상한 | 탐색 전략 자체가 달라짐 |
| **인내 예산** | 총 클릭 N회 / 총 시간 T분 초과 시 강제 「諦めた」 종료 | **이탈률 지표가 여기서 나온다** |

이게 있으면 재현 가능하고, 설명 가능하고, 기술 심사에서 방어된다.
없으면 "AI에게 역할극 시킨 것"이고, 그건 이미 있는 카테고리이며 비판받는 카테고리다.

### md 파일 30개에 대한 솔직한 평가
**판정 기준: "이 파일을 지우면 에이전트의 행동이 바뀌는가?"**
- 「오키나와 사투리를 쓴다」「출판업계 35년」 → 転入届 탐색 행동을 **바꾸지 않는다**
- 30개 중 25개는 이 테스트를 통과 못 할 것이다

단, **피치 자산으로서는 가치가 있다.** 그래프뷰를 보여주면 깊이 있어 보인다.
→ 정확도 자산과 데모 자산을 **혼동하지 말 것.** 행동을 바꾸는 5개를 정확히 알고, 나머지는 연출이라고 인식한 채로 쓸 것.

### 검증 부재 — 반드시 답을 준비할 질문
> **"그 시뮬레이션이 진짜 72세와 같다는 근거는?"**

#### 해법 1: 주장의 범위를 줄인다 (표현 수정)
- ❌ 「72歳のユーザーを再現したエージェント」 → 검증 불가능한 주장
- ⭕ **「72歳ユーザーの認知的・デジタル的制約の一部をモデル化したエージェント」**

#### 해법 2: 페르소나가 아니라 **규격(Spec)**으로 판다 ★핵심★

「페르소나」는 의심만 받을 수 있다. **「스펙」은 비판·버전관리·표준화가 가능하다.**
제약 조건을 공개 선언 가능한 명세로 만든다:

```yaml
profile: N3制約プロファイル v1.0
  vocabulary:  JLPT N3以下 + 常用カタカナ語リスト200語
  masking:     語彙外の単語は ◯ に置換
  viewport:    375x667 (스마트폰), zoom 100%
  tools:       Ctrl+F 禁止 / サイト内検索 禁止
  patience:    15クリック または 5分で「諦めた」
```

이렇게 하면:
- 「진짜 72세와 같은가」 → **답할 필요가 없어진다.** 우리는 사람을 흉내낸다고 주장하지 않는다
- 대신 **「이 명시된 제약 하에서 태스크가 실패했다」**를 주장한다 → 반증 가능하고 재현 가능하다
- 스펙은 **버전이 붙고, 공개되고, 비판받고, 개선된다** → 표준이 될 수 있는 형태
- 검증은 제품 출시를 막는 관문이 아니라, 나중에 답할 **연구 질문**이 된다

#### 해법 3: 소규모 실측 대조 (해커톤 내 실행 가능)
n=3이면 충분하다. 8/10~8/11에 **일찍** 할 것 — 결과가 피치를 바꾸기 때문.
1. 같은 미션 + 같은 사이트를 실제 사람 3명에게 시킨다
2. 화면 녹화 → 클릭 경로 추출
3. 에이전트 경로와 **겹쳐서 시각화**

- 겹치면 → 데모에서 가장 강력한 슬라이드가 된다
- 안 겹치면 → **피치 전에 알게 된다** (이게 더 중요)

**최속 경로:** 팀에 일본어 비원어민이 있다면 그 사람이 곧 N3 페르소나다. 오늘 당장 n=1 대조가 가능하다.

---

## 6. 리스크 포인트

### R1. 심사위원 적합성 ★가장 큰 위험★

`judges_research.md`(본인 작성)의 결론과 이 아이디어를 대조하면:

| 심사위원 | 기준 | 이 아이디어 |
|---|---|---|
| ちょまど | 진짜로 동작하고 데모로서 기분 좋은가 | ⭕ 강함. 에이전트가 헤매는 화면은 시각적으로 강렬. MS는 접근성에 진심 |
| かずなり | 한 줄로 설명 / 즉시 사용 / 공유하고 싶은가 | 🔺 국회에서 생성AI 사회구현 논의 이력 → 공공 DX는 개인 관심사와 직결. 단 "즉시 쓸 수 있는가"는 ❌ |
| mana | 현장이 계속 쓰는가 (정착) | ❌ 자치체가 매일 쓰는 툴이 아님. 연 1회 감사 도구 |
| みやび | **매출이 오르는가** | ❌ 완전히 안 맞음. 품질 개선은 이 사람 렌즈에서 가장 약한 카테고리 |

그리고 본인이 문서에 직접 써둔 **「피해야 할 것」**:
- ❌ 기술적으로 화려하지만 누가 쓰는지 불분명한 것
- ❌ 엔지니어만 이해하는 도구
- ❌ 「効率化」 단독 소구 (みやび 기준에서 약함)

**→ 이 아이디어는 본인이 정의한 회피 조건에 부분적으로 걸린다.**
評価5項目 중 「独自性・新規性」은 높게 나오지만 **「ビジネス価値」에서 실점**한다.
그리고 심사위원 4명 중 3명이 "일반인·중소기업이 AI를 쓰게 만드는 것"으로 먹고사는 사람들이다.

### R2. 페르소나 능력 제거 불가 (§5) — 기술적 치명. 제약 레이어로만 해결됨

### R3. 검증 부재 (§5) — 반증 불가능성. 계획만이라도 필요

### R4. SIer BM의 인센티브 역전

「NTT DATA, Fujitsu, TIS가 탐내지 않을까?」 → **역전이 있다.**

- SIer는 保守契約과 仕様変更으로 번다
- 자기 납품물이 42점이라는 **증거를 체계적으로 생성하는 도구**는 자기에게 불리한 발견 가능성을 만든다
- 자치체가 그 점수를 보면 첫 대화는 "개선 발주"가 아니라 **"왜 이렇게 납품했나"**가 된다
- 산다면 **통제하기 위해** 산다 (내부 전용, 비공개). 그러면 「일본 DX 개선」이라는 대의는 죽고, 그냥 내부 QA 툴이 된다
- 게다가 대기업 SIer 벤더 등록은 실적·자본금 요건 + 12~24개월 영업 사이클. 신규 팀에게는 잔혹하다
- **방어 불가:** 컨셉을 본 후지쯔가 3개월이면 내부 구축한다. md 파일과 프롬프트는 복제된다

**더 현실적인 구매자 후보 (우선순위 순)**
1. **리뉴얼 발주 직전의 자치체** — 사이트 리뉴얼은 5~7년 주기로 반드시 돈다. RFP를 쓰려면 現状評価가 필요하고 **그 예산은 이미 편성되어 있다.** 「リニューアル前の現状診断」은 팔 수 있는 형태
2. **외국인 주민 비율이 높은 자치체** — 新宿区, 港区, 浜松市, 大泉町 등은 **多文化共生 예산이 따로 있다.** "외국인이 転入届을 완수 가능한가" 지표가 기존 예산 항목에 직접 매핑된다 ← 가장 좋은 쐐기
3. デジタル庁 — 자치체 표준화 권한이 있어 횡단 비교 데이터를 원함. 단 조달 장벽
4. SIer — 위 이유로 후순위

### R5. 신규성 주장 과대 (§4) — 좁게 주장할 것

### R6. 「도달 판정」 엔지니어링 난제 (과소평가 중)
에이전트가 **"답을 찾았다"를 어떻게 판정하는가?** 이게 없으면 모든 지표가 무너진다.
- 미션별·사이트별 정답 키(ground truth)를 사람이 미리 만들거나
- LLM 심판이 판정하거나
- 둘 다 작업량이 크다. **9일 안에 미션 3개 × 사이트 5곳 = 15개 정답 키가 현실적인 상한**

### R7. 운영 지뢰
- **WAF** — 공공기관은 WAF 도입률이 높다. 기본 headless 지문은 차단/CAPTCHA. headed 모드 + 현실적 UA + 느린 페이싱. **데모 대상은 미리 돌려서 통과하는 곳으로 선정**
- **레이트 리밋 필수** — 도메인당 동시성 1, 요청 간 수 초, robots.txt 준수. 안 지키면 업무방해 구간
- **電子申請 SaaS 진입 금지** — ぴったりサービス, Graffer, LoGoフォーム 등. 로그인/신청이 있는 곳이 법적 경계선
- **라이브 크롤링 금지** — 무대에서 WAF 타임아웃으로 데모가 죽는다. 사전 녹화 필수, 라이브 버튼은 장식

### R8. browser-use 관측 파이프라인 훅 가능성 ★9일 프로젝트 최대 기술 급소★

browser-use를 인프라로 쓰는 판단은 옳다. 브라우저 자동화를 직접 만드는 데 시간을 태우면 안 된다.
**단, 구조적 충돌 가능성이 있다.**

- browser-use는 DOM에서 인터랙티브 요소를 추출해 **텍스트 목록으로 LLM에 넘긴다**
- 그런데 우리 제약 레이어의 본질은 **에이전트에게 가는 관측을 훼손하는 것**이다
  (어휘 마스킹 = 텍스트 개입 / 시야 제약 = DOM 텍스트 차단하고 스크린샷만)
- 즉 **browser-use의 관측 생성과 LLM 호출 사이에 끼어들어야 한다**

→ browser-use를 블랙박스로 쓰면 **제약 레이어를 못 만든다. 그러면 제품이 없다.**

**대응: 8/9 오전 2시간 스파이크. 이것부터 한다.**
- [ ] browser-use에서 LLM에 넘어가는 관측(state message)을 가로채 변형할 수 있는가?
- [ ] vision 모드에서 DOM 텍스트를 제외하고 스크린샷만 넘길 수 있는가?
- [ ] 가능 → browser-use 채택. 불가/난해 → **즉시 Playwright + 자체 얇은 루프로 전환**

자체 루프는 생각보다 작다: 스크린샷 + 클릭 가능 요소 목록 → LLM → 액션. 하루면 된다.
**판단을 미루면 3일을 태운다.**

### R9. 「AI 사용자 10명」의 분산 문제

「10人中7人が離脱」이라는 숫자를 만들려면 **10명이 서로 다르게 행동해야 한다.**
그런데 같은 스펙 + 같은 사이트 + 같은 LLM이면 거의 같은 경로를 간다.
→ 「10명」이 아니라 「1명 × 10회」가 되고, 이탈률이라는 지표 자체가 성립 안 한다.

**설계 단계에서 분산을 만들어야 한다:**
- 10명 = **서로 다른 5개 제약 프로필 × 2 변형** (다양성을 프로필 간 차이로 확보) ← 가장 확실
- 인내 예산 랜덤화 (12~20클릭)
- 진입 지점 다양화 (톱 페이지 / 검색 유입 / 직접 링크)
- temperature 상향

⚠️ 반대로 **10명이 전원 같은 곳에서 막히면 그것도 강한 메시지**다 (「10人中10人が同じ場所で脱落」).
어느 쪽이든 좋지만, **분산이 설계되어 있다는 것을 설명할 수 있어야** 한다. 질의에서 반드시 나온다.

---

## 7. 개선안 — 듀얼 타깃 구조 (권장)

**엔진은 하나, 타깃 URL만 다르다. 추가 개발 비용 거의 없음.**

```
              ┌─ [제품]  중소기업 웹사이트 / LP / EC / 예약사이트
페르소나 엔진 ─┤          → 「고객 페르소나 10명이 당신 사이트에서 어디서 이탈하는가」
              │          → 매출 직결. みやび / mana 렌즈 직격
              │
              └─ [화제성] 전국 자치체 사이트 일제 감사
                         → 「23区の区役所サイト、AIエージェントで全部踏破してみた」
                         → 사회적 의의 + 바이럴. かずなり 렌즈 직격
```

### 왜 이 구조인가

**A. 심사위원 4명 전원에게 걸린다**
- みやび: 전환율 = 매출. 그의 핵심 관심사(LP, 카피, 성약률)와 정확히 겹침
- mana: 중소기업이 계속 쓸 수 있는 도구. 정착 설계 가능
- かずなり: **「全国◯◯を全部やってみた結果まとめ」는 이 사람이 5년간 Qiita 1위를 먹은 형식 그 자체다.** 공공 감사 결과가 그대로 그 포맷이 된다
- ちょまど: 에이전트 리플레이 화면은 데모로서 강함

**B. 공공 감사가 마케팅 자산이 된다**
돈은 중소기업에서 받고, 화제성과 신뢰는 공공 감사로 만든다.
「전국 자치체 사이트를 전부 감사한 팀」이라는 실적이 영업 자산이 된다.
공공기관에 팔려고 애쓸 필요가 없어진다 → R4 인센티브 역전 문제가 **우회**된다.

**C. 조달 사이클을 안 기다려도 된다**
중소기업 LP 진단은 **내일 팔 수 있다.** 자치체는 12~24개월.

### 리스크
- 스코프 확대로 9일 안에 둘 다 못 만들 위험
  → **완화:** 엔진 공통, 페르소나 세트만 2종, 대시보드 1개. 실제 추가 비용은 페르소나 정의뿐

---

## 8. 포지셔닝

### 카테고리 정의: 「監査」가 아니라 「AI User Testing」

이 재정의가 프로젝트를 살린다.

| | 「사이트 監査」 | 「AI User Testing」 |
|---|---|---|
| 함의 | 컴플라이언스, 1회성, 비용센터 | 반복, 개선 사이클, 매출 |
| 구매자 | 정부·법무 | **마케팅·그로스** |
| 예산 | 감사 예산 (경직) | 광고·전환 예산 (유동) |
| mana·みやび 렌즈 | ❌ | ⭕ |

### browser-use와의 대비 — 피치의 핵심 슬라이드

> **Browser Useは、サイトを「使いこなす」エージェント。**
> **うちは、サイトを「使いこなせない」エージェント。**

한 줄로 차별점이 끝난다. 그리고 이 대비가 **제약 레이어가 왜 코어인지**를 동시에 설명한다.
- browser-use = 성공하려고 최선을 다한다 (능력 최대화)
- 이 제품 = 실패 지점을 찾기 위해 능력을 **의도적으로 제한한다** (능력 최소화)

**browser-use는 경쟁자가 아니라 기반 인프라다.** 9일밖에 없으므로 브라우저 자동화를 직접 만들지 않고
「AI User Testing이라는 제품 경험」에 시간을 몰빵한다. (단 §6-R8 급소 확인 필수)

### 진짜 경쟁자
browser-use가 아니라 **synthetic user research 카테고리**다 (해외 선행, 신뢰성 비판 중).
그들과의 차이는 단 하나:
> 그들은 **프롬프트로** 페르소나를 만든다. 우리는 **도구 계층에서 정보를 실제로 훼손한다.**

이 차이를 피치에서 명시적으로 대비시켜야 한다. 이것이 유일한 방어선이다.

### 이것이 아니다
- ❌ 접근성 검사 툴 (레드오션, 기성 벤더 존재, Lighthouse와 비교당함)
- ❌ 「AI 페르소나 UX 리서치」라는 표현 (선행 카테고리 연상 → 신뢰성 의심 유발)
- ❌ "AI가 쓸 수 있게 고치자" (투기적. 오늘 고객이 신경 쓰는 건 AI가 아니라 사람이다)

### 이것이다
- ⭕ **AI는 목적이 아니라 계측기다.** 지금까지 소수 표본·주관이던 사용성 평가를, 자동 피험자로 대규모·동일기준 측정한다
- ⭕ 산출물은 점수가 아니라 **재현 가능한 실패 트레이스**
- ⭕ 공공 벤치마크에서는 **「準拠している」와 「実際に使える」의 갭**을 드러낸다

> **이 문장 하나로 피치가 살고 죽는다:**
> ❌ "AI가 쓸 수 있는 사이트로 만듭시다"
> ⭕ "지금까지 주관이던 사용성을, AI를 자동 피험자로 써서 대규모로 객관 측정합니다"

---

## 9. 캐치프레이즈

### 본命 (공공 데모용)
> **「AA準拠」のサイトを、72歳は突破できなかった。**

강한 이유: 제도(準拠)와 현실(突破失敗)의 충돌이 한 줄에 들어감. 숫자도 감정도 있음.
⚠️ 주의: 실제로는 AI 시뮬레이션이므로 「72歳のAIペルソナ」가 정확하다. 슬라이드에서는 강한 카피를 쓰되 **방법론을 반드시 각주로 밝힐 것.** 감추면 質疑에서 무너진다.

### 변형
- **規格には合格した。市民には、まだ。**
- **主観だった「使いにくさ」を、AIエージェントが数字にする。**
- **AIエージェントが踏破できないサイトは、市民も踏破できない。**

### 제품용 (중소기업 타깃)
- **あなたのLP、10人の顧客ペルソナのうち7人が離脱しました。**
- **「なんとなく使いにくい」に、はじめて理由がつく。**

### ❌ 폐기한 것
- ~~「複雑さ」と「アナログ」というバグをAIエージェントで捕まえて改善する~~
  - 「改善する」 → 우리는 개선 안 한다. 측정한다. 스스로 함정을 판다
  - 「アナログ」 → 웹사이트는 이미 디지털. FAX 문제는 Playwright로 못 잡음. 스코프 불일치
  - 「バグ」 → 찾는 건 버그가 아니라 마찰. "그건 버그가 아니라 사양입니다"로 반박당함

### 서비스명 주의
「区役所ダンジョン」은 기억에 남지만 **고객(자치체)을 던전이라고 부르는 이름이다.** 팔 수 없다.
→ 대외 브랜드는 진지하게, 던전은 내부 코드네임으로.

---

## 10. 9일 실행 계획 (8/8 ~ 8/16)

**전제: 8/16 도쿄 현장 라이브 피치 (상위 5팀). 데모 영상은 예선 통과 티켓.**

| 일정 | 할 것 |
|---|---|
| 8/8 (Day1) | **테마 발표 확인** → 붙일 곳 확정 |
| **8/9 오전** | **★browser-use 훅 스파이크 (2h)★** — 관측 개입 가능한가? 불가 시 즉시 Playwright 자체 루프 (§6-R8) |
| 8/9~8/10 | 제약 레이어 구현. **어휘 마스킹 + 인내 예산**부터. 이게 제품의 심장 |
| 8/10 | 대상 사이트 5곳 선정 + **WAF 통과 사전 검증**. 미션 3개 정답 키 작성 |
| **8/10~8/11** | **★실사용자 n=3 대조★** — 늦추면 피치를 못 고친다 (§5 해법3) |
| 8/11~8/12 | 트레이스 기록 + 리플레이 뷰. 8/12 21시 **중간보고 반드시 참가** |
| 8/13 | 대시보드 + 개선안 자동 생성. 제약 프로필 5종 정의 (§6-R9 분산 확보) |
| 8/14 | **전 대상 사전 실행 + 녹화.** 라이브 크롤링 의존 금지 |
| 8/15 | 데모 영상 완성. 좌우 분할 리플레이가 승부처 |
| 8/16 | 현장 피치 |

### 質疑 대비 (반드시 준비)
| 질문 | 답 |
|---|---|
| "진짜 72세와 같나요?" | 같다고 주장하지 않는다. **명시된 제약 스펙 하의 결과**다 (§5 해법2) |
| "browser-use랑 뭐가 다르죠?" | 사용을 **잘하는** 에이전트 vs **못하는** 에이전트 (§8) |
| "Lighthouse랑 뭐가 다르죠?" | Lighthouse는 규칙, 이건 **태스크 완수 여부**. PDF 의존율·언어 패리티는 규칙 기반이 볼 수 없다 |
| "누가 돈 내죠?" | 중소기업 전환율 진단 (내일 팔 수 있음). 공공은 화제성 자산 (§7) |
| "왜 10명이죠?" | 5개 제약 프로필 × 2 변형. 분산 설계됨 (§6-R9) |

### 데모의 승부처
좌우 분할 리플레이. 왼쪽은 2클릭에 도달, 오른쪽은 9클릭 헤매다 포기.
클릭 경로를 선으로 그리고, 어휘 마스킹된 화면을 그대로 보여준다 (「オンライン◯◯」).
**아름다운 대시보드보다 이 30초가 중요하다.**

---

## 11. 확정해야 할 것

**즉시 (8/8~8/9)**
- [ ] **Day1 테마 발표 확인** → 이 아이디어가 붙을 곳이 있는지
- [ ] **★browser-use 훅 스파이크★** — 이게 안 되면 아키텍처가 바뀐다. 최우선 (§6-R8)
- [ ] 제약 프로필 5종 정의 (N3 외국인 / 고령자 / 스마트폰 초보 / 바쁜 직장인 / 첫 방문 고객)

**초반 (8/10~8/11)**
- [ ] 대상 사이트 5곳 선정 + WAF 사전 검증
- [ ] 실사용자 n=3 대조 — **늦추면 의미 없음**
- [ ] 어휘 마스킹용 사전 확보 (JLPT 등급표 / 카타카나 외래어 리스트)

**결정 사항**
- [x] 카테고리 = **AI User Testing** (監査 아님)
- [x] 듀얼 타깃 (B2B 매출 + 공공 화제성)
- [x] browser-use = 인프라 (경쟁자 아님)
- [ ] B2B와 공공 중 **데모 영상의 주인공**을 어느 쪽으로 할 것인가 ← 미결
- [ ] 서비스명 (「区役所ダンジョン」은 내부 코드네임으로만)
- [ ] 사전 개발 룰 확인 (운영에 문의 — judges_research.md 미해결 항목)
