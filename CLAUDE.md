# CLAUDE.md

## 이 프로젝트

**AI HACK 2026** 출품작. 팀 2인 (개발 1 / 디자인 1). 8/8 19:00 개막 → 8/16 결선.
**AI User Testing** — 제약이 강제된 AI 사용자를 웹사이트에 투입해 실제 고객이 어디서 이탈하는지 찾아낸다.

- 무엇을 왜 만드는지 → `AI User testing.md` (포지셔닝·리스크·9일 계획)
- 실측으로 확인된 것 → `FINDINGS.md` (**추측과 확인을 섞지 않기 위한 문서**)
- 우리가 주장하는 것과 주장하지 않는 것 → `profiles/README.md`
- 기각된 아이디어와 이유 → `../폐기.md` / 대회 전략 → `../hackathon_strategy.md`
- 보류 중인 선행 프로젝트 → `../外国人採用会社向けのトラブル解決/` (**손대지 않는다**)

## 절대 규칙

1. **LLM에게 가는 모든 바이트는 `constrain()`을 통과한다.** 우회로가 하나라도 생기면
   「연기하는 AI」로 돌아가고, 그건 이미 존재하고 이미 비판받는 카테고리다. 제품이 사라진다.
2. **근거 없이 마스킹하지 않는다.** 조사 미수록어는 통과시킨다(`unknown: "keep"`).
   오차는 항상 **과소 마스킹** 방향이어야 한다. 「이건 과소평가입니다」가 방어선이다.
3. **페이크 금지.** 수상하면 코드를 제출한다. 화면에 나오는 것은 실제로 동작해야 한다.
4. **읽기 전용.** 회원가입·마이넘버 인증·폼 제출·로그인·전자신청 SaaS 진입 금지.
   ToS 위반과 업무방해 리스크가 전부 거기서 발생한다. 공개 www 안에서만 움직인다.
5. **레이트 리밋.** 도메인당 동시성 1, 요청 간 4초 이상, robots.txt 준수.
6. **라이브 크롤링에 의존하지 않는다.** 무대에서 타임아웃 나면 데모가 죽는다. 사전 녹화 필수.
7. **8/13 기능 동결.** 이후는 영상만.

## 발표 금지어

> **「AIペルソナでUXリサーチ」「AIが72歳を再現」「サイト監査」「アクセシビリティ検査」**

각각 synthetic user research(신뢰성 비판 중) / 검증 불가능한 주장 / 감사 예산 프레임 / Lighthouse 비교당함.
→ ✅ 「**明示された制約スペックの下で**タスクが失敗した」 / 「主観だった使いにくさを**大規模に同一基準で計測**する」

## 표현의 선

| 하지 않는 주장 | 하는 주장 |
|---|---|
| 「72歳のユーザーを再現した」 | 「60歳以上の理解率30%未満の語を隠した状態で失敗した」 |
| 「実際の高齢者はこう行動する」 | 「このプロファイル v1.0 の再現結果である」 |
| 「この数字が母集団を代表する」 | 「n=◯の制約プロファイル実行結果である」 |

리포트에는 **항상 프로필 id + version**이 붙는다. 분리되는 순간 검증 불가능한 주장이 된다.
어휘 데이터의 한계(2002-2004년 조사·코호트 불일치)는 **먼저 말한다.** 감추면 質疑에서 무너진다.

## 구조

```
lexicon/         어휘 난이도 데이터 자산 — ✅ 완료
  vendor/          국어연 원본 CSV (CC BY 4.0, 재배포 가능)
  src/gairaigo.ts  원본 → JSONL
  src/mask.ts      ★ 마스킹 엔진. 이 제품의 심장
  data/gairaigo.jsonl  398어 (60+ 이해율 10% 미만 155어)
profiles/        ★ 제약 프로필 5종 = 우리의 주장 그 자체
agent/
  src/observe.ts   페이지 → RawObservation
  src/constrain.ts ★ RawObservation → Observation (제약 적용). 여기가 moat
  src/probe.ts     단일 페이지 before/after 확인
  src/sitecheck.ts 대상 사이트 사전 검증 (R7)
  data/sitecheck.json
```

## 명령

```bash
node lexicon/src/gairaigo.ts                    # 어휘 코퍼스 재빌드 (입력 고정 — 결과 동일)
node lexicon/src/mask.test.ts                   # 마스킹 엔진 35 케이스
node agent/src/probe.ts <url> [profile-id]      # 단일 페이지 before/after
node agent/src/sitecheck.ts                     # 5개 사이트 WAF·도달 확인
```

Node 25는 `.ts`를 그대로 실행한다. 빌드 스텝 없음.
브라우저는 **시스템 설치 Chrome** (`channel: "chrome"`). Playwright 번들 chromium은 지문이 달라 WAF에 걸리기 쉽다.

## 코드 규약

- 주석은 **왜**를 쓴다. 무엇을 하는지는 코드가 말한다.
- 한국어 주석 + 일본어 도메인 용어. 코드·식별자는 영어.
- **Node 타입 스트립 모드 제약**: constructor 파라미터 프로퍼티(`constructor(private x: T)`) 불가.
  enum·namespace·데코레이터도 불가. 필드를 명시한다.
- `page.evaluate`에 문자열을 넘길 때는 **즉시 호출로 감싼다** (`` `(${FN})()` ``).
  안 그러면 함수 객체가 직렬화돼 `undefined`가 돌아온다.
- 마스킹 히트에는 **항상 근거 숫자와 출처**가 붙는다 (`evidence()`). 근거 없는 히트는 버그다.
- 데이터 출처와 라이선스는 `lexicon/vendor/`에 원본째로 남긴다. CC BY는 표시 의무가 있다.

## 미결 (발안자 담당)

- **사전 개발 허용 범위의 정확한 룰 확인** — 현재 만든 것은 전부 테마 무관 데이터 자산 + 인프라다.
  제품 UI·대시보드는 만들지 않았다.
- Day1 테마 발표 후 붙일 곳 확정
- B2B(중소기업 LP)와 공공 중 **데모 영상의 주인공** 결정
- 서비스명 (「区役所ダンジョン」은 내부 코드네임으로만 — 고객을 던전이라 부르는 이름은 팔 수 없다)
